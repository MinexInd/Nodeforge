/**
 * NodeForge public API — Node registration and definition.
 *
 * <p>This package contains the developer-facing API for registering custom
 * node types with NodeForge. Node definitions describe a node's ports,
 * properties, behavior, and visual appearance.
 */
package net.minex.nodeforge.api.registry;
