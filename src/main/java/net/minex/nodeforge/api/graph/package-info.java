/**
 * NodeForge public API — Graph model interfaces.
 *
 * <p>This package contains the stable, public-facing interfaces for the graph
 * data model: graphs, nodes, ports, and connections. Implementations reside
 * in {@code net.minex.nodeforge.core.graph}.
 *
 * <p><strong>API Stability:</strong> Classes in the {@code api} package hierarchy
 * are considered stable within a major version. Breaking changes will only
 * occur across major version boundaries.
 */
package net.minex.nodeforge.api.graph;
