/**
 * NodeForge client — Animation system.
 *
 * <p>Provides reusable animation primitives (tweens, easing, springs, etc.)
 * for nodes, connections, overlays, and transitions. Designed to avoid
 * per-frame allocations.
 */
package net.minex.nodeforge.client.animation;
